# smarthr360-core-hr — employee & organization microservice
Django==5.2.8
djangorestframework>=3.15,<4.0
drf-spectacular>=0.27,<1.0
django-cors-headers>=4.3,<5.0
python-decouple>=3.8,<4.0
dj-database-url>=2.1,<3.0
gunicorn>=21.2,<22.0
whitenoise>=6.6,<7.0
psycopg2-binary>=2.9,<3.0

# Shared platform JWT client (RS256 local verification).
# Shared platform JWT client — vendored wheel (built from smarthr360-platform/packages/smarthr360-jwt-auth).
./smarthr360_jwt_auth-1.1.1-py3-none-any.whl

# Observability
django-prometheus>=2.3

# Skill-gap prediction — ML path (Étape 4). Optional at runtime: the analyzer
# falls back to a pure-Python least-squares trend when these are absent, but
# installing them enables the scikit-learn LinearRegression path.
numpy>=1.26,<3.0
pandas>=2.1,<3.0
scikit-learn>=1.4,<2.0
